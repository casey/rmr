

      /*
      dz     = -axes[2] * 0.05;
      droll  = -axes[5] * 0.0001 * rmr.TAU;

      dy     = -axes[1] * 0.01 + axes[3] * 0.005;
      dpitch = -axes[3] * 0.0001 * rmr.TAU - axes[1] * rmr.TAU * 0.00005;

      dx     =  axes[0] * 0.01 + axes[4] * 0.005;
      dyaw   =  axes[4] * 0.0001 * rmr.TAU - axes[0] * rmr.TAU * 0.00005;

      dpitch = -dpitch;

      dz = droll * 100;
      droll = dyaw;
      */
