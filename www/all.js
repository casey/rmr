'use strict';

define(function(require) {
  var rmr = require('rmr');

  require('array' );
  require('string');
  require('number');

  require('camera');
  require('node'  );
  require('color' );
  require('key'   );
  require('ct'    );
  require('stats' );
  require('glod'  );
  require('midi'  );
  require('cmd'   );

  return rmr;
});
